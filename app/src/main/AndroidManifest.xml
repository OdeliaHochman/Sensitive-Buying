<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sensitivebuying">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".RepresentativeStatistics"></activity>
        <activity android:name=".RepresentativeProductDetails" />
        <activity android:name=".RepresentativeAddProduct" />
        <activity android:name=".RepresentativeSearch" />
        <activity android:name=".RepresentativeHome" />
        <activity android:name=".CustomerSearchActivity"
            android:theme="@style/AppTheme.NoActionBar"/>
        <activity android:name=".FavoritesActivity" />
        <activity android:name=".ContactActivity" />
        <activity android:name=".UpdateSensitivesActivity" />
        <activity android:name=".CustomerDetailsActivity" />
        <activity android:name=".SensitiveRegisterActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".OpenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>